[pytest]
# asyncio_mode=auto lets `async def test_*` run without a per-test decorator.
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
# Put the repo root on sys.path so `import app` works when pytest is run as the
# bare console script (as CI does), not just via `python -m pytest`.
pythonpath = .
